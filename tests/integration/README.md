# Integration test specification

Initial integration scenarios cover release admission, route compilation, deployment switching, generic invocation, capability binding, transactional state commit, outbox persistence, effect dispatch, node route watching, and exact-release remote invocation.
