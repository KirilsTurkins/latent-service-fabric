Original recovery JSON, API journal and helper bytes from the zero-Invoke smoke03 cleanup completion. The original-runtime NDJSON retains the last four original API lines with their original ordinals. Cleanup JSON uses the original producer canonical serialization and matches the retained original receipt. These small fixtures test cleanup proof only; they do not qualify a workload or execute the retained helper.
