# User-space state pager

Investigate Linux `userfaultfd` for page-on-demand state materialization and dirty-page delta capture. The portable runtime continues to use explicit state capability calls. Arbitrary language heaps are out of scope unless their representation is stable and explicitly designed for persistence.
