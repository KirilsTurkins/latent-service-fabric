# Call-graph fusion

Investigate runtime-derived composite artifacts for stable local call chains. Fusion keys must include every release, binding, policy, runtime, compiler, and resource-accounting input. Logical tracing and failure semantics must remain visible after fusion.
