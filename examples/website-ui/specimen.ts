// Synthetic typescript UI fixture. Never executed as an SDK example.
// lsf-example-begin: specimen
const text = '<script>window.__lsfUnsafeExample = true</script> — 東京';
// Indentation, Unicode and this deliberately long line remain intact when the selected snippet is copied; long code scrolls within its own container.
// lsf-example-end: specimen
