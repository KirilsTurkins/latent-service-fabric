# Contract-only examples

Examples define WIT contracts and declarative LSF resources without providing guest or runtime implementations.

- `echo-contract`: stateless request/response service.
- `counter-contract`: transactional keyed-state service.
- `order-workflow-contract`: durable workflow-facing service contract.
