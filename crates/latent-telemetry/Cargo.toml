[package]
name = "latent-telemetry"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Bounded node-owned telemetry, activation observation, and structured local export."

[lints]
workspace = true

[dependencies]
latent-core = { path = "../latent-core" }
latent-activation = { path = "../latent-activation" }
tokio.workspace = true

[dev-dependencies]
latent-routing = { path = "../latent-routing" }
