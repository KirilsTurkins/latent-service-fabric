# Benchmark program

Benchmarks validate LSF claims against persistent HTTP microservices and worker-process plugin models. Results must include environment, engine version, CPU features, cache state, payload size, and confidence intervals.
