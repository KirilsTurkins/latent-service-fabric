name: Stage Issue 22 correction bundle

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  staged:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "The correction bundle is being staged."
