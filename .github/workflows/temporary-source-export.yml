name: Apply and validate Issue 22 corrections

on:
  pull_request:
    branches:
      - development

permissions:
  contents: write

concurrency:
  group: issue-22-correction-${{ github.event.pull_request.number }}
  cancel-in-progress: false

jobs:
  correct:
    if: >-
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.event.pull_request.head.ref == 'feat/issue-22-containment-runner'
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
        with:
          repository: ${{ github.event.pull_request.head.repo.full_name }}
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: 3.13.5
          cache: pip
          cache-dependency-path: tools/requirements.lock

      - name: Install Python validator dependencies
        run: python -m pip install --requirement tools/requirements.lock

      - uses: dtolnay/rust-toolchain@1.97.1
        with:
          components: rustfmt, clippy
          targets: wasm32-wasip2,wasm32-unknown-unknown

      - uses: bytecodealliance/actions/wasm-tools/setup@v1
        with:
          version: 1.254.0

      - uses: bufbuild/buf-setup-action@v1
        with:
          version: 1.72.0

      - name: Apply the reviewed correction bundle
        shell: bash
        run: |
          set -euo pipefail
          base64 --decode .github/lsf22-correction.patch.gz.b64 | gzip --decompress > /tmp/issue-22-corrections.patch
          git apply --check /tmp/issue-22-corrections.patch
          git apply /tmp/issue-22-corrections.patch
          rm .github/lsf22-correction.patch.gz.b64
          rm .github/workflows/temporary-source-export.yml
          cargo fmt --all
          git diff --check

      - name: Validate repository structure
        run: python tools/validate_repository.py

      - name: Check the complete workspace
        run: cargo check --workspace --all-targets --all-features --locked

      - name: Run Clippy for the complete workspace
        run: cargo clippy --workspace --all-targets --all-features --locked

      - name: Run scheduler tests
        run: cargo test -p latent-scheduler --all-targets --locked

      - name: Run the complete workspace test suite
        run: cargo test --workspace --all-targets --all-features --locked

      - name: Build and execute the real Component Model containment fixtures
        run: tools/validate_contracts.sh

      - name: Commit the validated corrections
        shell: bash
        run: |
          set -euo pipefail
          git diff --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add --all
          git commit -m "fix: complete issue 22 containment acceptance coverage"
          git push origin "HEAD:${{ github.event.pull_request.head.ref }}"
